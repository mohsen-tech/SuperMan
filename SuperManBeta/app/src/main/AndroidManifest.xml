<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.intech.supermanbeta">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".first_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".asli_page" />
        <activity android:name=".vorod_or_sabt_name" />
        <activity android:name=".moarefi_resturant" />
        <activity android:name=".menu_market" />
        <activity android:name=".list_market_near_me" />
        <activity android:name=".list_market_near_btnboro" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity"></activity>
    </application>

</manifest>